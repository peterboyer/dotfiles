- defaults:
    link:
      create: true
      relink: true

- link:
    # zsh
    ~/.zshrc: ./zsh/zshrc.macos
    # git
    ~/.gitconfig: ../dotfiles.untracked/git/gitconfig
    # npm
    ~/.npmrc: ../dotfiles.untracked/npm/npmrc
    # yarn
    ~/.yarnrc: ../dotfiles.untracked/yarn/yarnrc
