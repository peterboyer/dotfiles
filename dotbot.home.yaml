- defaults:
    link:
      create: true
      relink: true

- link:
    # fonts
    ~/.local/share/fonts/berkeley-mono: ../fonts/berkeley-mono
    # signal
    ~/.config/Signal: ../dotfiles.untracked/signal
    # ngrok
    ~/.config/ngrok: ../dotfiles.untracked/ngrok
