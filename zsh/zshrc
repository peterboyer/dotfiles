#!/usr/bin/env bash

export GIT_SERVE_DIR=~/_/mirrors

. ~/_/dotfiles/zsh/self.shared

# fnm, node/npm/yarn/etc.
eval "$(fnm env --use-on-cd)"

# nodebox, node/npm/yarn/etc.
alias npm="nodebox npm"
alias npx="nodebox npx"
alias yarn="nodebox yarn"
